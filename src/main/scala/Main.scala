object Main {
  def main(args: Array[String]): Unit = {
    var range = 10
    var random = scala.util.Random
    // val k = random.nextInt(range) // TODO change back to random
    val k = 3
    println(k)
  }
}
